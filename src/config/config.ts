const config = {
  movies: {
    api: {
      baseUrl: 'http://www.omdbapi.com/'
    },
    /* Debouncing wait for search text input (miliseconds) */
    inputDebounce: 1000
  }
}

export default config;